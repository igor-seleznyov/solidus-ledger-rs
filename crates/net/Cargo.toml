[package]
name = "net"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
mio = { workspace = true }
protocol = { path = "../protocol" }
ringbuf = { path = "../ringbuf" }
pipeline = { path = "../pipeline" }
config = { path = "../config" }

[dev-dependencies]
ledger = { path = "../ledger" }