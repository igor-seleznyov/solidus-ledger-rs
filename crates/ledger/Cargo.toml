[package]
name = "ledger"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
common = { path = "../common" }
ringbuf = { path = "../ringbuf" }
pipeline = { path = "../pipeline" }
protocol = { path = "../protocol" }
storage = { path = "../storage" }